# Talon UCAV Aerodynamics Simulation
**Team SkyGuard Alliance** | *Supported by YTB (Yurtdışı Türkler ve Akraba Topluluklar Başkanlığı)*

This repository contains a high-fidelity Computational Fluid Dynamics (CFD) environment for the Talon UCAV. It is optimized for analyzing lift, drag, and pressure distribution at cruise velocities using **OpenFOAM 2512**.

---

## Technical Overview

| Feature | Specification |
| :--- | :--- |
| **Solver** | `simpleFoam` (Steady-state incompressible Navier-Stokes) |
| **Turbulence Model** | k-Omega SST (Shear Stress Transport) |
| **Mesh Density** | ~1.7 Million cells (Hex-dominant) |
| **Flow Velocity** | 20 m/s (Sea Level Conditions) |

---

## Prerequisites

You must have **Docker** installed. Pull the optimized SkyGuard engineering environment from Docker Hub:

```bash
docker pull allrider1127/ytb_teknofest_ucav
```

## EXECUTION


**1. Mesh Generation**

Extract surface features and generate the refined hex-dominant mesh:
```bash

surfaceFeatureExtract
blockMesh
snappyHexMesh -overwrite
```

**2. Domain Decomposition**

Prepare the mesh for parallel processing (set to 4 cores by default):

```bash
decomposePar
```

**3. Solving the Physics**

Execute the solver using MPI:
```bash

mpirun -np 4 simpleFoam -parallel
```

**4. Post-Processing**

Stitch the parallel results back together for visualization:
```bash
reconstructPar
touch view.foam
```

**Open view.foam in ParaView to analyze the results.**

Core Engineering Concepts


    - k-Omega SST: Utilized for its superior ability to predict flow separation and adverse pressure gradients on airfoil surfaces.
    - Mesh Refinement: High-density cell clustering on leading edges and trailing wakes to capture the "Stagnation Point" and boundary layer physics accurately.
    - Wake Analysis: Analysis of the velocity deficit in the wake region allows for iterative fuselage optimization to minimize parasitic drag.

<img width="3999" height="1860" alt="image" src="https://github.com/user-attachments/assets/0904fe96-28d6-4162-b93b-260cf0794011" />


## Visualization Note

The Docker container is optimized for headless compute. For visualization:
1. Ensure **ParaView** is installed on your local host.
2. Run `reconstructPar` in Docker to merge results.
3. Use the `view.foam` dummy file to open the directory in your local ParaView.


## Aerodynamic Analysis Results (OpenFOAM)

The following performance data was extracted from the converged steady-state results using `simpleFoam` with a **k-omega SST** turbulence model.

### Aerodynamic Coefficients (Cruise @ 20 m/s)
| Parameter | Symbol | Value | Notes |
| :--- | :--- | :--- | :--- |
| **Lift Coefficient** | $C_L$ | **0.3236** | Sufficient for cruise at 20 m/s. |
| **Drag Coefficient** | $C_D$ | **0.0324** | Optimized profile drag. |
| **Lift-to-Drag Ratio** | $L/D$ | **9.99** | High aerodynamic efficiency. |
| **Pitching Moment** | $C_m$ | **0.2395** | Positive static stability margin. |
| **Pressure Drag** | $C_{d(p)}$ | **0.0287** | 88.5% of total drag. |
| **Viscous Drag** | $C_{d(v)}$ | **0.0037** | Skin friction component. |

### Convergence Proof
Simulation converged at **1000 iterations** with the following residuals:
* **Pressure Residual (p):** $1.8 \times 10^{-6}$.
* **Velocity Residual (Ux):** $2.1 \times 10^{-6}$.
* **Continuity Error:** $2.4 \times 10^{-9}$.

![COnvergence Residuals](<img width="1920" height="1080" alt="image" src="https://github.com/user-attachments/assets/7844ad43-343c-4373-896b-315a835cc774" />
)

### Flow Field Visualization

#### Surface Pressure Distribution ($p$)
The pressure field visualization confirms the generation of a high-pressure zone (red) at the stagnation point of the nose and a significant suction zone (blue) on the upper wing surface, validating the calculated $C_L$ of 0.3236.

![Pressure Field](<img width="1600" height="900" alt="image" src="https://github.com/user-attachments/assets/046dbe9a-1f02-4d35-b701-73cbb7e3dee1" />
)

#### Velocity Field and Surface LIC ($U$)
Using Line Integral Convolution (LIC) and velocity magnitude mapping, we analyzed the flow attachment over the fuselage and wings. The results show smooth flow over the cockpit and primary lifting surfaces at 20 m/s.

![Velocity Field](<img width="1600" height="900" alt="image" src="https://github.com/user-attachments/assets/deacbb8b-3654-47c1-9707-31a131ff66e4" />
)

#### Mesh Quality & Boundary Layer
The mesh proof shows the hex-dominant structure generated by `snappyHexMesh`. High-density refinement zones are visible around the leading edges to accurately resolve the boundary layer physics.

![Mesh Quality](<img width="1600" height="900" alt="image" src="https://github.com/user-attachments/assets/331b4ce1-de6c-4fdb-9235-db389dfde881" />
)


### Extraction Command
To reproduce these coefficients from the latest time-step, run the following in the project root:
```bash
simpleFoam -postProcess -latestTime
```
